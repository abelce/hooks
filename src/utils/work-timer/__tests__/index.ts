// import {getWorkerTimer} from "../helper";

describe('work-timer', () => {
  it('', () => {
    // const fn = jest.fn(() => {})
    // getWorkerTimer().setTimeout(fn, 1000)
    // jest.advanceTimersByTime(1000);
    // expect(fn).toBeCalledTimes(1);
  });
});
